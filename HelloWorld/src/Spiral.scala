import Element.elem

object Spiral {
  val space = elem(" ")
  val cornel = elem("+")
  def spiral(nEdges: Int, direction: Int): Element = {
    if (nEdges == 1)
      elem("+")
    else {
      val sp
    }

  }
}
